'use client';

// Placeholder for net worth trend chart
// Will use household_snapshots data when available

export function NetWorthChart() {
  return (
    <div className="h-[200px] flex items-center justify-center text-sm text-muted-foreground">
      순자산 추이 차트 (데이터 수집 후 표시)
    </div>
  );
}
